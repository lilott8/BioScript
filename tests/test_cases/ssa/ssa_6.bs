// correct ssa
// nested while

module mod
stationary stat
manifest aaa
manifest bbb
manifest ccc
manifest ddd

functions:

instructions:

x = dispense aaa
x = dispense ddd
x = dispense aaa

while (3 > 3)
{
    x = dispense bbb

    while (4 > 4)
    {
        x = dispense ccc
    }

    a = detect mod on x
}

b = detect mod on x


